# CS336 Lecture 1 笔记
##在相同浮点运算次数量制约下，模型规模与训练次数显然构成反比关系，此时通过改变模型参数寻找能使loss最小的情况，并记录此时的模型参数。发现**总浮点运算次数和最优模型参数（使loss最小）成统计线性关系**，这符合常识但又出人意料。
##按字符编码的话会有稀疏性，按字节编码又会序列太长，按单词编码会使词汇表无限长，而**BPE：不预设任何规则，直接在具体文本上训练，尝试维护哈夫曼规则，先按字节编码，然后寻找高频字节对，重新编译字节对为新的字节**